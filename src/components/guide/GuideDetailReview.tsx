import { View, Text, StyleSheet } from "react-native";

function GuideDetailReview() {
  return (
    <View style={styles.container}>
      <Text>GuideDetailReview</Text>
    </View>
  );
}

export default GuideDetailReview;

const styles = StyleSheet.create({
  container: {
    width: "20%",
    height: 200,
    borderWidth: 1,
  },
});
