import { View, Text, StyleSheet } from "react-native";

function GuideDetailSchedule() {
  return (
    <View style={styles.container}>
      <Text>GuideDetailSchedule</Text>
    </View>
  );
}

export default GuideDetailSchedule;

const styles = StyleSheet.create({
  container: {
    width: "20%",
    height: 200,
    borderWidth: 1,
  },
});
